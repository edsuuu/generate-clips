yt-dlp>=2025.1.15
faster-whisper>=1.1.0
# Transcrição na GPU (Metal) — instalado só em macOS Apple Silicon; ignorado no resto.
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"
# Transcrição na GPU (CUDA) no Linux/NVIDIA: o ctranslate2 carrega libcublas.so.12
# e libcudnn.so.9 por soname. Estes wheels fornecem essas libs; o
# app/support/cuda_bootstrap.py as injeta no LD_LIBRARY_PATH em runtime.
nvidia-cublas-cu12; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cudnn-cu12; sys_platform == "linux" and platform_machine == "x86_64"
ffmpeg-python>=0.2.0
rich>=13.9.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
httpx>=0.28.0
python-dotenv>=1.0.1
minio>=7.2.0

# Face tracking + Active Speaker Detection
mediapipe>=0.10.18
opencv-python>=4.10.0
scipy>=1.14.0
librosa>=0.10.2

# API HTTP (microsserviço chamado pelo Laravel) — stateless, sem banco
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# Optional LLM providers (install only the ones you need)
# anthropic>=0.40.0
# google-generativeai>=0.8.0
# openai>=1.59.0
